import {TOAST_SUCCESS, TOAST_FAILURE } from "../actions";

const initialState = {
    isSuccessToastOpen: false,
    isErrorToastOpen: false
};

export default function(state = initialState, action) {
    switch (action.type) {

    }
}